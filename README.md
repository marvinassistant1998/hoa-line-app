# 管委會 AI 虛擬總幹事 - LINE LIFF App

台灣小型社區（30-150戶）管委會專用的 LINE LIFF 應用程式，自動化管委會的重複性行政工作。

## 功能特色

- 📊 **首頁儀表板** - 待辦事項、管理費收繳進度、維修追蹤
- 👥 **住戶管理** - 列表、搜尋、篩選、繳費歷史、LINE 催繳
- 🏢 **廠商管理** - 列表、詳情、單據上傳、服務紀錄、評價
- ⚙️ **設定頁** - 方案管理、使用量統計、管理者管理

## 技術架構

- **框架**: Vite + React + TypeScript
- **樣式**: Tailwind CSS
- **狀態管理**: Zustand
- **LINE 整合**: @line/liff

## 專案結構

```
hoa-line-app/
├── src/
│   ├── components/     # 共用元件
│   │   ├── Icons.tsx   # SVG 圖示元件
│   │   ├── ui/         # UI 元件（TabBar, Header, Card 等）
│   │   └── index.ts
│   ├── screens/        # 頁面元件
│   │   ├── HomeScreen.tsx
│   │   ├── ResidentsScreen.tsx
│   │   ├── VendorsScreen.tsx
│   │   └── SettingsScreen.tsx
│   ├── stores/         # Zustand 狀態管理
│   ├── services/       # API 服務（LIFF）
│   ├── types/          # TypeScript 型別
│   ├── data/           # Mock 資料
│   ├── App.tsx
│   └── main.tsx
├── public/
└── ...config files
```

## 開發指南

### 安裝依賴

```bash
npm install
```

### 設定環境變數

複製 `.env.example` 為 `.env` 並填入您的 LIFF ID：

```bash
cp .env.example .env
```

編輯 `.env`：
```
VITE_LIFF_ID=your-liff-id
```

### 本地開發

```bash
npm run dev
```

開啟瀏覽器訪問 http://localhost:5173

### 建置生產版本

```bash
npm run build
```

## LINE LIFF 設定

1. 前往 [LINE Developers Console](https://developers.line.biz/)
2. 建立或選擇一個 Provider
3. 建立一個 LINE Login Channel
4. 在 LIFF 標籤頁建立新的 LIFF App
5. 設定 Endpoint URL（部署後的 URL）
6. 複製 LIFF ID 到 `.env` 檔案

## 部署

### Vercel 部署

1. 將專案推送到 GitHub
2. 連結 Vercel 和 GitHub repo
3. 設定環境變數 `VITE_LIFF_ID`
4. 部署

### 其他平台

建置後的靜態檔案在 `dist/` 目錄，可部署到任何靜態網站託管服務。

## 下一步開發

### Phase 2 - 後端整合
- [ ] 設定 Firebase/Supabase
- [ ] 實作 Authentication
- [ ] 建立資料庫結構
- [ ] 實作 CRUD API

### Phase 3 - 進階功能
- [ ] 會議錄音 + AI 轉文字
- [ ] AI 財務報表產生
- [ ] 推播通知

## License

Private - All Rights Reserved
